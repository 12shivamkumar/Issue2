CREATE TABLE `campaign_delivery_status_112_val` (
  `status_id` bigint(20) NOT NULL,
  `veneno_status_label` varchar(100) NOT NULL,
  `campaign_legend_status_id` int(11) NOT NULL,
  `campaign_legend_lebel` varchar(100) NOT NULL,
  PRIMARY KEY (`status_id`)
) ENGINE=InnoDB ;

CREATE TABLE supplementary_membership_cycle_details (`id` int(11) NOT NULL AUTO_INCREMENT, `org_id` int(11) NOT NULL, `loyalty_program_id` int(11) NOT NULL, `partner_program_id` int(11) NOT NULL, `cycle_type` enum('DAYS','MONTHS') COLLATE utf8mb4_unicode_ci NOT NULL DEFAULT 'DAYS', `cycle_value` int(11) NOT NULL, created_on DATETIME NOT NULL, `auto_update_time` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP, PRIMARY KEY (`id`));
